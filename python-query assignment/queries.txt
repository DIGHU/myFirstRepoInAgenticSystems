-- Create Table
CREATE TABLE students (
    id INT PRIMARY KEY,
    name TEXT,
    score INT,
    passed BOOLEAN,
    class INT
);


INSERT INTO students (id, name, score, passed, class) VALUES
(1, 'Aarav', 92, TRUE, 10),
(2, 'Diya', 76, TRUE, 10),
(3, 'Rohan', 65, FALSE, 9),
(4, 'Meera', 88, TRUE, 10),
(5, 'Kabir', 54, FALSE, 9),
(6, 'Ananya', 95, TRUE, 10),
(7, 'Rahul', 81, TRUE, 9),
(8, 'Sneha', 73, TRUE, 9),
(9, 'Arjun', 84, TRUE, 10),
(10, 'Kavya', 69, FALSE, 9);


-- Queries
SELECT * FROM students;

SELECT * FROM students WHERE passed = TRUE;

SELECT * FROM students WHERE passed = FALSE;

SELECT * FROM students ORDER BY score DESC;

SELECT * FROM students ORDER BY score DESC LIMIT 1;

SELECT AVG(score) FROM students;

SELECT id, name, score,
RANK() OVER (ORDER BY score DESC) AS rank
FROM students;

SELECT * FROM students WHERE class = 10;